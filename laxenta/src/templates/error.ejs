<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Error - <%= botName %></title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500&display=swap" rel="stylesheet" />
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      background: #121212;
      color: rgb(219, 18, 18);
      font-family: 'Inter', sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      overflow: hidden;
      position: relative;
    }
    .background {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(135deg, #ff4444, #005bb5);
      clip-path: circle(70% at 50% -20%);
      animation: pulse 5s infinite alternate ease-in-out;
    }
    @keyframes pulse {
      from { transform: scale(1); opacity: 0.9; }
      to { transform: scale(1.1); opacity: 1; }
    }
    .error-container {
      position: relative;
      background: rgba(30, 30, 30, 0.9);
      border-radius: 12px;
      padding: 2rem;
      width: 90%;
      max-width: 500px;
      text-align: center;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.5);
      animation: fadeIn 0.6s ease-in-out;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-30px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .error-icon {
      font-size: 5rem;
      color: #ff4444;
      margin-bottom: 1rem;
    }
    .error-title {
      font-size: 2rem;
      font-weight: 500;
    }
    .error-message {
      color: #b9bbbe;
      margin: 1rem 0;
    }
    .back-button {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.75rem 1.5rem;
      background: #0070f3;
      color: #fff;
      text-decoration: none;
      border-radius: 6px;
      font-weight: 500;
      transition: background 0.2s ease-in-out, transform 0.2s;
    }
    .back-button:hover {
      background: #005bb5;
      transform: scale(1.05);
    }
    .glow {
      position: absolute;
      width: 200px;
      height: 200px;
      background: rgba(255, 68, 68, 0.4);
      filter: blur(80px);
      border-radius: 50%;
      animation: moveGlow 6s infinite alternate ease-in-out;
    }
    @keyframes moveGlow {
      from { transform: translate(-50px, -50px); }
      to { transform: translate(50px, 50px); }
    }
  </style>
</head>
<body>
  <div class="background"></div>
  <div class="glow" style="top: 20%; left: 10%;"></div>
  <div class="glow" style="bottom: 15%; right: 10%;"></div>
  
  <div class="error-container animate__animated animate__fadeInDown">
    <i class="fas fa-exclamation-triangle error-icon"></i>
    <h1 class="error-title">Oops! Something went wrong</h1>
    <p class="error-message"><%= error || 'An internal error happened, U dont need to know the details, the dev will, u can retry with discord, ngl its rare that it will fix it' %></p>
    <a href="/auth/discord" class="back-button">Login with Discord</a>
  </div>
</body>
</html>
